<div class="form-group">
    <label class="control-label col-sm-2" for="{{name}}">{{label}}{{#if mandatory}}<span class="mandatory">*</span>{{/if}}</label>
    <div class="col-sm-10">
        <input class="form-control" type="{{type}}" name="{{name}}" placeholder="{{label}}" {{#if focus}}autofocus{{/if}} />
    </div>
</div>
